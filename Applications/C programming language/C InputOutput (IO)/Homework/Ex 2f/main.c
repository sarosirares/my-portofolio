#include <stdio.h>
int main()
{
    int int_num1 = 0, int_num2 = 0; scanf("%d%d", &int_num1, &int_num2); // sscanf is used when we want to read formatted imput from a character string, variable names are wrong in scanf function
    return 0;
}
