#include <stdio.h>
int main()
{
    int x = 0, y = 0; printf("The sum of %d and %d is %d.\n", x, y, x+y); //We have to put the text between "" and then we have to put the variables that we want to use
    return 0;
}
